export interface Hero {
  // I don't like not prefacing an interface with an I but apparently that's wrong in Typescript?
  // The tutorial states to do it this way so that is what is implemented.

  id: number;
  name: string;
}
